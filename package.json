{
  "name": "proxy-tests",
  "version": "0.2.1",
  "description": "Proxy Tests",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "rm -rf out && electron-zip-packager . --out out --platform=darwin,linux,win32 --arch=ia32,x64 --overwrite"
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "Microsoft",
  "devDependencies": {
    "electron": "^1.4.1",
    "electron-zip-packager": "^4.0.1",
    "eslint": "^3.12.2"
  },
  "dependencies": {
    "http-proxy-agent": "^1.0.0",
    "https-proxy-agent": "^1.0.0"
  }
}
